{
  "name": "brewery-server-component",
  "version": "1.0.0",
  "description": "Backend component for the Brewery management system",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev",
    "dev": "cross-env NODE_ENV=development nodemon --exec babel-node --presets babel-preset-env app.js",
    "build": "npm run prod",
    "prod": "cross-env NODE_ENV=production node app.js"
  },
  "author": "Arun K Ramachandran",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "express-session": "^1.17.1",
    "method-override": "^3.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.1.2",
    "chai-http": "^4.0.0",
    "cross-env": "^7.0.2",
    "mocha": "^5.1.1",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
